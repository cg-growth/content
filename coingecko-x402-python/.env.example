# CoinGecko X402 Environment Variables
# =====================================
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# SECURITY WARNING:
#   - NEVER commit your .env file to version control
#   - Use a test wallet with minimal funds
#   - Each API call costs $0.01 USDC

# =============================================================================
# NETWORK SELECTION (Optional)
# =============================================================================
# If you have both keys set, the script will auto-detect.
# Set NETWORK to force a specific network:
#   NETWORK=evm      (use Base network)
#   NETWORK=solana   (use Solana network)
#
# NETWORK=auto

# =============================================================================
# OPTION 1: EVM (Base Network)
# =============================================================================
# Your EVM wallet private key for Base network
# This wallet must have USDC on Base network to pay for API calls
#
# Format: with or without 0x prefix
#   EVM_PRIVATE_KEY=0x1234567890abcdef...
#   EVM_PRIVATE_KEY=1234567890abcdef...
#
# IMPORTANT: Replace this placeholder with your actual private key!
# The placeholder value below will NOT work.
#
EVM_PRIVATE_KEY=your_evm_private_key_here

# =============================================================================
# OPTION 2: Solana Network
# =============================================================================
# Your Solana wallet private key (base58 encoded)
# This wallet must have USDC on Solana to pay for API calls
#
# Format: 64-byte base58 encoded secret key
#   SOLANA_PRIVATE_KEY=5abc123...your_base58_key...xyz789
#
# Note: You'll also need to install Solana support:
#   pip install 'x402[svm]'
#
# SOLANA_PRIVATE_KEY=your_solana_private_key_here

# =============================================================================
# NOTES
# =============================================================================
#
# Getting USDC on Base:
#   1. Bridge from Ethereum: https://bridge.base.org/
#   2. Buy directly on Coinbase and withdraw to Base
#
# Getting USDC on Solana:
#   1. Buy on a Solana-supported exchange
#   2. Use a faucet for devnet testing: https://faucet.circle.com/
#
# Payment Cost:
#   - Each API call costs $0.01 USDC
#   - Running all 5 tests = ~$0.05 USDC
